<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ================================================================
         HARMONIC insight Design System - Ivory & Gold Theme
         https://github.com/HarmonicInsight/lib-insight-common
         ================================================================ -->

    <!-- ============================================================ -->
    <!-- Background Colors                                            -->
    <!-- ============================================================ -->
    <Color x:Key="BgPrimaryColor">#FAF8F5</Color>
    <Color x:Key="BgSecondaryColor">#F3F0EB</Color>
    <Color x:Key="BgCardColor">#FFFFFF</Color>
    <Color x:Key="BgElevatedColor">#F7F5F1</Color>
    <Color x:Key="BgHoverColor">#EEEBE5</Color>
    <Color x:Key="BgActiveColor">#E8E4DC</Color>
    <Color x:Key="BgTitlebarColor">#2C2820</Color>
    <Color x:Key="BgOverlayColor">#661C1917</Color>

    <SolidColorBrush x:Key="BgPrimaryBrush" Color="{StaticResource BgPrimaryColor}"/>
    <SolidColorBrush x:Key="BgSecondaryBrush" Color="{StaticResource BgSecondaryColor}"/>
    <SolidColorBrush x:Key="BgCardBrush" Color="{StaticResource BgCardColor}"/>
    <SolidColorBrush x:Key="BgElevatedBrush" Color="{StaticResource BgElevatedColor}"/>
    <SolidColorBrush x:Key="BgHoverBrush" Color="{StaticResource BgHoverColor}"/>
    <SolidColorBrush x:Key="BgActiveBrush" Color="{StaticResource BgActiveColor}"/>
    <SolidColorBrush x:Key="BgTitlebarBrush" Color="{StaticResource BgTitlebarColor}"/>

    <!-- ============================================================ -->
    <!-- Accent Colors (Gold) - Brand Primary                         -->
    <!-- ============================================================ -->
    <Color x:Key="Accent50Color">#FAF5E8</Color>
    <Color x:Key="Accent100Color">#F0E6C8</Color>
    <Color x:Key="Accent200Color">#E8D5A3</Color>
    <Color x:Key="Accent300Color">#D4B96E</Color>
    <Color x:Key="Accent400Color">#C9A84C</Color>
    <Color x:Key="Accent500Color">#B8942F</Color>
    <Color x:Key="Accent600Color">#A68325</Color>
    <Color x:Key="Accent700Color">#8C711E</Color>
    <Color x:Key="Accent800Color">#6E5918</Color>
    <Color x:Key="Accent900Color">#5C4A12</Color>
    <Color x:Key="AccentMutedColor">#14B8942F</Color>
    <Color x:Key="AccentSubtleColor">#0AB8942F</Color>

    <SolidColorBrush x:Key="Accent50Brush" Color="{StaticResource Accent50Color}"/>
    <SolidColorBrush x:Key="Accent100Brush" Color="{StaticResource Accent100Color}"/>
    <SolidColorBrush x:Key="Accent200Brush" Color="{StaticResource Accent200Color}"/>
    <SolidColorBrush x:Key="Accent300Brush" Color="{StaticResource Accent300Color}"/>
    <SolidColorBrush x:Key="Accent400Brush" Color="{StaticResource Accent400Color}"/>
    <SolidColorBrush x:Key="Accent500Brush" Color="{StaticResource Accent500Color}"/>
    <SolidColorBrush x:Key="Accent600Brush" Color="{StaticResource Accent600Color}"/>
    <SolidColorBrush x:Key="Accent700Brush" Color="{StaticResource Accent700Color}"/>
    <SolidColorBrush x:Key="Accent800Brush" Color="{StaticResource Accent800Color}"/>
    <SolidColorBrush x:Key="Accent900Brush" Color="{StaticResource Accent900Color}"/>
    <SolidColorBrush x:Key="AccentMutedBrush" Color="{StaticResource AccentMutedColor}"/>
    <SolidColorBrush x:Key="AccentSubtleBrush" Color="{StaticResource AccentSubtleColor}"/>

    <!-- Accent Gradient -->
    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#C9A84C" Offset="0"/>
        <GradientStop Color="#B8942F" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="AccentGradientHoverBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#D4B96E" Offset="0"/>
        <GradientStop Color="#C9A84C" Offset="1"/>
    </LinearGradientBrush>

    <!-- ============================================================ -->
    <!-- Semantic Colors - Status                                     -->
    <!-- ============================================================ -->
    <Color x:Key="Success600Color">#16A34A</Color>
    <Color x:Key="Success700Color">#15803D</Color>
    <Color x:Key="SuccessBgColor">#0F16A34A</Color>

    <Color x:Key="Warning600Color">#CA8A04</Color>
    <Color x:Key="Warning700Color">#A16207</Color>
    <Color x:Key="WarningBgColor">#0FCA8A04</Color>

    <Color x:Key="Error600Color">#DC2626</Color>
    <Color x:Key="Error700Color">#B91C1C</Color>
    <Color x:Key="ErrorBgColor">#0FDC2626</Color>

    <Color x:Key="Info600Color">#2563EB</Color>
    <Color x:Key="Info700Color">#1D4ED8</Color>
    <Color x:Key="InfoBgColor">#0D2563EB</Color>

    <SolidColorBrush x:Key="Success600Brush" Color="{StaticResource Success600Color}"/>
    <SolidColorBrush x:Key="Warning600Brush" Color="{StaticResource Warning600Color}"/>
    <SolidColorBrush x:Key="Error600Brush" Color="{StaticResource Error600Color}"/>
    <SolidColorBrush x:Key="Info600Brush" Color="{StaticResource Info600Color}"/>
    <SolidColorBrush x:Key="SuccessBgBrush" Color="{StaticResource SuccessBgColor}"/>
    <SolidColorBrush x:Key="WarningBgBrush" Color="{StaticResource WarningBgColor}"/>
    <SolidColorBrush x:Key="ErrorBgBrush" Color="{StaticResource ErrorBgColor}"/>
    <SolidColorBrush x:Key="InfoBgBrush" Color="{StaticResource InfoBgColor}"/>

    <!-- ============================================================ -->
    <!-- Category Colors (for icons/badges)                           -->
    <!-- ============================================================ -->
    <Color x:Key="CatPptxColor">#D97706</Color>
    <Color x:Key="CatPptxBgColor">#0FD97706</Color>
    <Color x:Key="CatExcelColor">#16A34A</Color>
    <Color x:Key="CatExcelBgColor">#0F16A34A</Color>
    <Color x:Key="CatJsonColor">#2563EB</Color>
    <Color x:Key="CatJsonBgColor">#0D2563EB</Color>
    <Color x:Key="CatFolderColor">#B8942F</Color>
    <Color x:Key="CatFolderBgColor">#14B8942F</Color>
    <Color x:Key="CatCompareColor">#7C3AED</Color>
    <Color x:Key="CatCompareBgColor">#0D7C3AED</Color>

    <SolidColorBrush x:Key="CatPptxBrush" Color="{StaticResource CatPptxColor}"/>
    <SolidColorBrush x:Key="CatPptxBgBrush" Color="{StaticResource CatPptxBgColor}"/>
    <SolidColorBrush x:Key="CatExcelBrush" Color="{StaticResource CatExcelColor}"/>
    <SolidColorBrush x:Key="CatExcelBgBrush" Color="{StaticResource CatExcelBgColor}"/>
    <SolidColorBrush x:Key="CatJsonBrush" Color="{StaticResource CatJsonColor}"/>
    <SolidColorBrush x:Key="CatJsonBgBrush" Color="{StaticResource CatJsonBgColor}"/>
    <SolidColorBrush x:Key="CatFolderBrush" Color="{StaticResource CatFolderColor}"/>
    <SolidColorBrush x:Key="CatFolderBgBrush" Color="{StaticResource CatFolderBgColor}"/>
    <SolidColorBrush x:Key="CatCompareBrush" Color="{StaticResource CatCompareColor}"/>
    <SolidColorBrush x:Key="CatCompareBgBrush" Color="{StaticResource CatCompareBgColor}"/>

    <!-- ============================================================ -->
    <!-- Text Colors                                                  -->
    <!-- ============================================================ -->
    <Color x:Key="TextPrimaryColor">#1C1917</Color>
    <Color x:Key="TextSecondaryColor">#57534E</Color>
    <Color x:Key="TextTertiaryColor">#A8A29E</Color>
    <Color x:Key="TextDisabledColor">#D6D3D1</Color>
    <Color x:Key="TextAccentColor">#8C711E</Color>
    <Color x:Key="TextOnAccentColor">#FFFFFF</Color>
    <Color x:Key="TextOnDarkColor">#F3F0EB</Color>
    <Color x:Key="TextOnDarkSecondaryColor">#99F3F0EB</Color>
    <Color x:Key="TextOnDarkTertiaryColor">#59F3F0EB</Color>

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextTertiaryBrush" Color="{StaticResource TextTertiaryColor}"/>
    <SolidColorBrush x:Key="TextDisabledBrush" Color="{StaticResource TextDisabledColor}"/>
    <SolidColorBrush x:Key="TextAccentBrush" Color="{StaticResource TextAccentColor}"/>
    <SolidColorBrush x:Key="TextOnAccentBrush" Color="{StaticResource TextOnAccentColor}"/>
    <SolidColorBrush x:Key="TextOnDarkBrush" Color="{StaticResource TextOnDarkColor}"/>
    <SolidColorBrush x:Key="TextOnDarkSecondaryBrush" Color="{StaticResource TextOnDarkSecondaryColor}"/>
    <SolidColorBrush x:Key="TextOnDarkTertiaryBrush" Color="{StaticResource TextOnDarkTertiaryColor}"/>

    <!-- ============================================================ -->
    <!-- Border Colors                                                -->
    <!-- ============================================================ -->
    <Color x:Key="BorderDefaultColor">#E7E2DA</Color>
    <Color x:Key="BorderStrongColor">#D6D0C6</Color>
    <Color x:Key="BorderSubtleColor">#F0ECE5</Color>
    <Color x:Key="BorderAccentColor">#40B8942F</Color>
    <Color x:Key="BorderAccentStrongColor">#80B8942F</Color>
    <Color x:Key="BorderFocusColor">#B8942F</Color>
    <Color x:Key="BorderErrorColor">#DC2626</Color>

    <SolidColorBrush x:Key="BorderDefaultBrush" Color="{StaticResource BorderDefaultColor}"/>
    <SolidColorBrush x:Key="BorderStrongBrush" Color="{StaticResource BorderStrongColor}"/>
    <SolidColorBrush x:Key="BorderSubtleBrush" Color="{StaticResource BorderSubtleColor}"/>
    <SolidColorBrush x:Key="BorderAccentBrush" Color="{StaticResource BorderAccentColor}"/>
    <SolidColorBrush x:Key="BorderAccentStrongBrush" Color="{StaticResource BorderAccentStrongColor}"/>
    <SolidColorBrush x:Key="BorderFocusBrush" Color="{StaticResource BorderFocusColor}"/>
    <SolidColorBrush x:Key="BorderErrorBrush" Color="{StaticResource BorderErrorColor}"/>

    <!-- ============================================================ -->
    <!-- Button Styles                                                -->
    <!-- ============================================================ -->

    <!-- Default Button Style (Secondary) -->
    <Style TargetType="Button">
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderStrongBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="1" BlurRadius="2" Opacity="0.05" Color="Black"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BgElevatedBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Accent500Brush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource BgElevatedBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextDisabledBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Primary Button Style (Gold Accent) -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="20,8"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="{StaticResource AccentGradientBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource Accent700Brush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.12" Color="#B8942F"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentGradientHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource BgActiveBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextDisabledBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Header Button Style -->
    <Style x:Key="HeaderButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="14,6"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderStrongBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="1" BlurRadius="2" Opacity="0.03" Color="Black"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BgElevatedBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Accent500Brush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Ghost Button Style -->
    <Style x:Key="GhostButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BgHoverBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Danger Subtle Button Style -->
    <Style x:Key="DangerSubtleButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ErrorBgBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource Error600Brush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================================ -->
    <!-- TextBox Style                                                -->
    <!-- ============================================================ -->

    <Style TargetType="TextBox">
        <Setter Property="Padding" Value="14,9"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="1" BlurRadius="2" Opacity="0.03" Color="Black"/>
                        </Border.Effect>
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource BorderStrongBrush}"/>
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource BorderFocusBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================================ -->
    <!-- ComboBox Style                                               -->
    <!-- ============================================================ -->

    <Style TargetType="ComboBox">
        <Setter Property="Padding" Value="14,9"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Height" Value="38"/>
        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- ============================================================ -->
    <!-- Slider Style                                                 -->
    <!-- ============================================================ -->

    <Style TargetType="Slider">
        <Setter Property="Height" Value="24"/>
    </Style>

    <!-- ============================================================ -->
    <!-- CheckBox Style                                               -->
    <!-- ============================================================ -->

    <Style TargetType="CheckBox">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- ============================================================ -->
    <!-- Expander Style                                               -->
    <!-- ============================================================ -->

    <Style TargetType="Expander">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12"/>
    </Style>

    <!-- ============================================================ -->
    <!-- TabItem Style                                                -->
    <!-- ============================================================ -->

    <Style TargetType="TabItem">
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="{StaticResource TextAccentBrush}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================================ -->
    <!-- TabControl Style                                             -->
    <!-- ============================================================ -->

    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- ============================================================ -->
    <!-- Label Style                                                  -->
    <!-- ============================================================ -->

    <Style TargetType="Label">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="Padding" Value="0,0,0,6"/>
    </Style>

    <!-- Section Label Style (uppercase, no background) -->
    <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
        <Setter Property="Padding" Value="12,12,12,6"/>
    </Style>

    <!-- ============================================================ -->
    <!-- Plan Badge Style                                             -->
    <!-- ============================================================ -->

    <Style x:Key="PlanBadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AccentMutedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderAccentBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="100"/>
        <Setter Property="Padding" Value="10,3"/>
    </Style>

    <Style x:Key="PlanBadgeTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextAccentBrush}"/>
    </Style>

    <!-- Pro Tag Style -->
    <Style x:Key="ProTagStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AccentMutedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderAccentBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="6,2"/>
    </Style>

    <Style x:Key="ProTagTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="9"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource Accent500Brush}"/>
    </Style>

    <!-- ============================================================ -->
    <!-- Menu Styles (for Titlebar)                                   -->
    <!-- ============================================================ -->

    <Style TargetType="Menu">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextOnDarkSecondaryBrush}"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource TextOnDarkSecondaryBrush}"/>
        <Setter Property="Padding" Value="10,3"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="#14FFFFFF"/>
                <Setter Property="Foreground" Value="{StaticResource TextOnDarkBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================================ -->
    <!-- Status Dot Style                                             -->
    <!-- ============================================================ -->

    <Style x:Key="StatusDotOnlineStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="7"/>
        <Setter Property="Height" Value="7"/>
        <Setter Property="Fill" Value="{StaticResource Success600Brush}"/>
    </Style>

    <Style x:Key="StatusDotWarningStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="7"/>
        <Setter Property="Height" Value="7"/>
        <Setter Property="Fill" Value="{StaticResource Warning600Brush}"/>
    </Style>

    <Style x:Key="StatusDotErrorStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="7"/>
        <Setter Property="Height" Value="7"/>
        <Setter Property="Fill" Value="{StaticResource Error600Brush}"/>
    </Style>

</ResourceDictionary>
